# 学习笔记

课程设计第一阶段D1-D2的学习记录。主要学了四块内容：AI 工具的使用方法、harness 和模型的选择、git 的原理和功能、以及课程设计的选题调研。

## 一、AI 工具的学习

### 1.1 我对 vibe coding 的理解

以前我写代码是"想一句写一句"，vibe coding 相当于是反过来：**先用人话把要做什么说清楚，让 AI 来写，我负责检查它写得对不对**。刚开始我以为这就是"让 AI 帮我写作业"，实际试了几次之后发现完全不是——需求没说清楚的话，AI 生成的东西根本没法用，所以人的工作量并没有减少，只是从"写代码"转移到了"想需求、定标准、查问题"上。

### 1.2 提示词工程：怎么跟 AI 提需求

看 B 站教程加上自己试，总结了三点：

**① 任务要拆小。** 我一开始试过直接说"帮我做一个设备管理系统"，AI 一口气生成了一堆文件，看起来很美，但跑不起来，也不知道从哪改。后来改成一次只让它做一件事，比如"先帮我建数据库的表"，做完验证没问题再做下一个，顺畅多了。

**② 约束要说死。** 比如明确说"后端用 FastAPI 不要用 Flask""数据表就叫 equipment"，不然 AI 每次生成的风格都不一样，前后对不上。

**③ 说清楚怎么算做完。** 比如"接口用 curl 测试要返回 JSON"，这样我自己验收的时候也有标准，不是"看起来差不多就行"。

举个对比，同样是提需求：

- 差的说法：`帮我写个设备管理后端` → AI 自由发挥，十有八九跑偏；
- 好的说法：`用 FastAPI+SQLite 写设备台账的增删改查接口，表有 id、编号、型号、投运日期、状态五个字段，写完告诉我怎么用 curl 测试` → 生成的东西基本能直接用。

### 1.3 上下文工程

这个是我之前完全不知道的概念。意思是：AI 每次能"看到"的内容是有限的，所以要主动管理给它看什么。两个实用做法：

- 项目里放一个规则文件（我打算叫 `PROJECT_RULES.md`），写好技术栈、目录结构，让 AI 改代码前先读它，保持风格统一；
- **让 AI 先读现有代码再改**，不要让它凭空重写。不然经常出现"新功能是有了，旧功能被改没了"的情况。

### 1.4 规格驱动开发

流程大概是：

```
我先写需求文档 → AI 根据文档列实现计划 → 我确认 → 分步实现 → 每步都运行验证
```

核心就是"文档先行"，先写清楚再动手，而不是边做边想。老师要求的《需求规格说明书》正好可以一物两用：既是提交物，又是给 AI 的"总规则"。

### 1.5 AI 写的代码怎么保证靠谱

- **写自动化测试**：用 pytest 给接口写测试用例，改完代码跑一遍，防止新功能把旧功能弄坏（任务书也要求项目必须通过自带的自动化测试）；
- **逐行读懂再提交**：老师说了答辩会随机指代码段让我们讲，所以 AI 生成的代码我一定自己过一遍，看不懂的就问、就查，实在搞不懂的写法就让 AI 换个简单写法。**看不懂的代码坚决不放进去。**

### 1.6 AI 改不好的时候怎么办（人工接管）

教程里讲的调试接管策略，我理解就是三条：

1. **回滚**：git 回到上一个能跑的版本，别在错误代码上越改越乱；
2. **缩小改动**：一次只改一个地方；
3. **最小复现**：把出问题的那段单独抽出来跑，搞清楚到底哪行错，再拿着具体报错去问 AI——直接说"你的代码报错了"是没用的，要说"第几行报了什么错"。

## 二、拟选用的 harness 和模型

### 2.1 先搞懂 harness 是什么

说实话这个词我一开始不懂，查了之后才明白：**模型本身只会聊天，harness 是把模型"武装"起来的工具框架**——有了它，AI 才能读我项目里的文件、帮我改代码、执行命令。同一个模型装在不同的 harness 里，干活能力是不一样的，所以任务书里 harness 和模型是分开选的。

### 2.2 我的选择

| 项目    | 选择                                  |
| ------- | ------------------------------------- |
| Harness | **Kimi Code**（VSCode 插件）          |
| 模型    | **Kimi K3**（任务书推荐模型清单里有） |

### 2.3 为什么这么选

任务书列的候选工具有 ZCode、Trae、Qwen Code、通义灵码、Deepseek Harness、Claude Code、Codex、Open Code，我挑了几个了解了一下：

- **Trae**：是个独立的 IDE，功能挺全，但我 VSCode 已经用习惯了，不想再换一个软件重新适应；
- **Claude Code / Codex**：网上评价很好，但要折腾网络还要付费订阅，学生党劝退；
- **通义灵码 / Qwen Code**：也是 VSCode 插件，国内用着方便，留作备选；
- **Kimi Code**：VSCode 插件，装完就能用，配的是 K3 模型。我平时用 Kimi 查资料比较多，中文交流顺畅，长上下文也适合"让它先读整个项目再改"的用法。

综合考虑：**主力用 Kimi Code + K3，如果中途不好用就换通义灵码或 Qwen Code。**

### 2.4 给自己定的使用规矩

- 每次让 AI 写代码，把关键的 prompt 和结果截图存到仓库的 `prompt日志/` 文件夹（过程考核要交）；
- AI 写的代码我看懂、测试过了才 commit；
- 专门记一下 AI 出错的案例和我怎么纠正的（设计报告里有"AI 使用披露"这一章，到时候直接用）。

## 三、Git 的原理和功能

### 3.1 git 是干嘛的

版本控制工具，记录代码的每一次修改，改错了能退回去。是"分布式"的，意思是每个人电脑上都存着完整的修改历史，断网也能用。

### 3.2 最重要的概念：三个区

这个是我觉得 git 里最核心的东西，画个图：

```
工作区 --git add--> 暂存区 --git commit--> 本地仓库 --git push--> GitHub
```

- **工作区**：我正在改的文件；
- **暂存区**：add 之后相当于放进"购物车"，可以先攒着；
- **本地仓库**：commit 之后才算真正存了一个版本；
- **push** 之后 GitHub 上才能看到。

我之前一直搞不懂 add 和 commit 的区别，理解成"先放进购物车、再下单"就通了。

### 3.3 常用命令（实操中记的）

| 命令                   | 作用                                       |
| ---------------------- | ------------------------------------------ |
| `git clone 网址`       | 把仓库下载到电脑                           |
| `git add 文件名`       | 放进暂存区                                 |
| `git commit -m "说明"` | 提交一个版本，说明要写清楚                 |
| `git push origin main` | 传到 GitHub                                |
| `git status`           | 看看现在什么状态（不知道怎么办就先敲这个） |
| `git log`              | 看提交历史                                 |

补充两个名词：`origin` 就是 GitHub 上那个仓库的代号（clone 的时候自动设好的），`main` 是主分支的名字。我一开始以为 origin 是个要单独配置的东西，折腾了半天。

### 3.4 分支和 .gitignore

- 分支就是开一条"平行世界"改代码，改好了再合回来，互不干扰。我这个项目一个人做，先用 main 就够了；
- `.gitignore` 文件里写哪些东西不上传，比如 `node_modules/`（这个文件夹特别大）、Python 的 `__pycache__/` 这些。

### 3.5 为什么要小步提交

考核要看 git 提交记录，要求小步提交、提交说明写清楚。我自己的体会是：**每次 commit 就是存一个存档点**，后面改崩了能退回来。提交说明写"完成设备台账接口"这种，过两周自己也能看懂；写"update"这种等于没写。

### 3.6 我的实操过程

1. 注册 GitHub 账号，建了仓库；
2. 配置身份：`git config --global user.name "hanyin-png"` 和 `user.email`（填注册邮箱）；
3. clone 仓库到本地 G 盘；
4. 第一次 push 的时候弹出来浏览器授权页面，登录同意之后才推上去，当时还以为报错了；
5. 仓库名一开始随便起的，后来改成了 `Vibecodingproject`，旧链接会自动跳转，但接龙要发新链接。

## 四、选题调研

### 4.1 选的什么题

**工业设备智能运维与预测性维护平台**。任务书要求场景必须是制造/工业环节，我选的是"设备运维"这个环节。

### 4.2 为什么选这个题

有两个原因：

一是这个方向本身有意义。工厂设备维修经历了"坏了才修 → 定期检修 → 预测性维护"的发展。预测性维护就是用传感器数据和 AI 模型预测设备"还能用多久"，在坏之前安排维修，既不浪费也不过度。这是智能制造里"智能服务"的典型应用，和课程内容直接对应。

二是我自己有相关经历。上学期参加机器人比赛的时候，我调试过一台机械臂不动弹的故障，当时自己摸索出一套排查方法：先看上层软件报什么错，再检查硬件供电和状态，最后直接发底层指令一层层定位。**这个"分层排查"的思路正好可以做成平台里"故障诊断建议"功能的规则库**，算是把自己的实践经验用上了。

### 4.3 数据集调研

预测性维护方向有个经典的公开数据集：**NASA 的 C-MAPSS 涡扇发动机退化数据集**，很多论文都用它。

- 内容是很多台发动机从崭新一直跑到报废的完整数据，每行是一台发动机工作一次的记录，有 3 个工况参数和 21 个传感器读数（温度、压力、转速这些）；
- 分训练文件（完整寿命，用来训练模型）和测试文件（只跑到一半，模拟正在服役的设备，用来做演示）；
- NASA 官方渠道要填表申请，比较麻烦，但飞桨 AI Studio、魔搭、GitHub 上都有现成的，我准备从这几个地方下载；
- 它有好几个子集，我选最简单的 FD001（100 台发动机），够用了。

### 4.4 准备用哪些技术（对应课程内容，至少 3 章）

| 技术              | 在系统里干什么                                                                   |
| ----------------- | -------------------------------------------------------------------------------- |
| 机器学习          | 用随机森林预测设备剩余寿命（RUL）                                                |
| 深度学习          | 用 LSTM 处理传感器的时间序列，和随机森林的效果做对比                             |
| 专家系统/规则推理 | 故障诊断规则引擎：匹配到异常模式就给出排查建议（规则库结合我自己的排查经验来写） |
| 数据挖掘          | 用孤立森林做异常检测，发现异常就触发预警                                         |

这四个模块不是各干各的，是串成一条线的：**异常检测先发现问题 → 寿命预测判断严不严重 → 触发预警 → 规则引擎给维修建议 → 生成维修工单**。任务书要求技术方向不能是装饰性的，这样设计每个模块都有实际作用。

### 4.5 系统打算怎么做

- 整体是 B/S 架构（浏览器打开就能演示）；
- 前端：Vue3 + Element Plus + ECharts 画图表，做设备台账、趋势曲线、健康度仪表盘、预警、诊断、工单六个页面；
- 后端：FastAPI（Python 写的，和算法部分一个语言，方便直接调用）；
- 数据库：SQLite（不用单独装数据库软件，课设规模够用）；
- 模型提前训练好存成文件，系统运行的时候只负责调用出结果，这样演示的时候不会卡。

### 4.6 看了哪些同类项目

在 GitHub 上搜 "CMAPSS RUL" 看了一些开源项目，发现大部分只是跑模型出预测值的脚本，没有做成能用的系统。我的课程设计想做到的区别是：**把算法装进一个完整的业务流程里**（设备台账 → 数据监测 → 预警 → 诊断 → 工单），而不是只交一个模型实验。

## 五、下一步计划（D3~D5）

1. 写《需求规格说明书》（功能清单、页面草图、架构图），同时当作给 AI 的项目规则文件；
2. 把 C-MAPSS 数据集下载下来，看懂数据格式，准备 D6 的清洗入库；
3. 把前端 Vue 脚手架和后端 FastAPI 的空架子搭起来跑通。

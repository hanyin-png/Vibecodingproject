# 工业设备智能运维与预测性维护平台 · 需求规格说明书

| 项目 | 内容 |
|---|---|
| 课程 | 制造智能技术课程设计 |
| 开发方式 | vibe coding（AI 协作开发，Kimi Code + Kimi K3） |
| 版本 | v1.0（D3~D5 阶段） |

---

## 1. 引言

### 1.1 项目背景

制造企业的设备运维模式正在经历三个阶段演进：**事后维修**（坏了才修，停机损失大）→ **定期维修**（按周期检修，容易过度维修或维修不足）→ **预测性维护 PdM**（根据传感器数据评估设备健康状态，在故障发生前安排维修）。

本项目面向制造场景的"设备运维"环节，使用 NASA C-MAPSS 涡扇发动机退化数据集，开发一套设备健康管理系统，展示预测性维护的完整业务流程。

### 1.2 项目目标

构建一个 B/S 架构的可运行系统，实现业务闭环：

> 设备台账 → 传感器数据监测 → 健康评估（RUL 预测）→ 异常预警 → 智能诊断建议 → 维修工单

系统综合运用《制造智能技术》课程的机器学习、深度学习、专家系统、数据挖掘四个技术方向，每个方向在业务流程中承担实际功能。

### 1.3 术语

| 术语 | 全称 | 含义 |
|---|---|---|
| PHM | Prognostics and Health Management | 故障预测与健康管理 |
| RUL | Remaining Useful Life | 剩余使用寿命，本项目中单位为"循环数" |
| PdM | Predictive Maintenance | 预测性维护 |
| C-MAPSS | Commercial Modular Aero-Propulsion System Simulation | NASA 涡扇发动机仿真数据集 |

---

## 2. 需求概述

### 2.1 目标用户

制造企业设备运维工程师：负责监测设备运行状态、安排检修计划、处理维修工单。

### 2.2 业务闭环流程

```
① 设备台账：录入/导入设备（100 台涡扇发动机）
→ ② 数据监测：查看设备 21 路传感器的趋势曲线，异常点标红
→ ③ 健康评估：点击"健康评估"，RUL 模型返回剩余寿命与健康度评分
→ ④ 异常预警：异常检测命中 / RUL 低于阈值 → 自动生成预警记录
→ ⑤ 智能诊断：对预警点击"智能诊断"，规则引擎输出故障原因与分层排查建议
→ ⑥ 维修工单：一键生成工单，状态流转（待处理 → 维修中 → 已完成）
```

---

## 3. 功能需求

系统共 6 个页面，功能点如下（优先级：P0 = 必须有，P1 = 重要，P2 = 可简化）：

### 3.1 设备台账页

| 编号 | 名称 | 描述 | 优先级 |
|---|---|---|---|
| F1.1 | 设备列表 | 展示设备编号、型号、投运日期、健康状态 | P0 |
| F1.2 | 设备增删改 | 手动新增、编辑、删除设备 | P0 |
| F1.3 | 批量导入 | 从 C-MAPSS 数据集批量导入 100 台设备 | P0 |

### 3.2 数据监测页

| 编号 | 名称 | 描述 | 优先级 |
|---|---|---|---|
| F2.1 | 传感器趋势曲线 | 选择设备后，ECharts 展示有趋势的传感器曲线（如 s3/s7/s11/s12） | P0 |
| F2.2 | 异常点标红 | 异常检测命中的数据点在曲线上标红 | P1 |
| F2.3 | 循环范围筛选 | 支持按 cycle 范围过滤数据 | P1 |

### 3.3 健康评估页

| 编号 | 名称 | 描述 | 优先级 |
|---|---|---|---|
| F3.1 | RUL 预测 | 点击按钮触发随机森林模型推理，展示剩余寿命（循环数） | P0 |
| F3.2 | 健康度评分 | 按公式 `health = RUL/125×100` 展示 0~100 评分 | P0 |
| F3.3 | LSTM 对比预测 | 展示 LSTM 模型的 RUL 预测结果，与随机森林对比 | P1（可降级为报告数据） |
| F3.4 | 历史预测记录 | 展示该设备的历史评估记录 | P1 |

### 3.4 预警中心页

| 编号 | 名称 | 描述 | 优先级 |
|---|---|---|---|
| F4.1 | 预警列表 | 展示设备、预警类型（传感器异常/剩余寿命不足）、级别、时间、状态 | P0 |
| F4.2 | 自动生成预警 | 异常检测命中或 RUL ≤ 90 时自动生成预警记录 | P0 |
| F4.3 | 标记处理 | 预警状态从"未处理"改为"已处理" | P0 |

### 3.5 智能诊断页

| 编号 | 名称 | 描述 | 优先级 |
|---|---|---|---|
| F5.1 | 规则引擎诊断 | 对预警点击"智能诊断"，输出疑似故障部位 | P0 |
| F5.2 | 分层排查建议 | 输出"先软后硬、先外后内"的分层排查步骤与维修措施 | P0 |

### 3.6 维修工单页

| 编号 | 名称 | 描述 | 优先级 |
|---|---|---|---|
| F6.1 | 一键生成工单 | 从预警生成工单，自动带入诊断建议 | P0 |
| F6.2 | 工单状态流转 | 待处理 → 维修中 → 已完成 | P0 |
| F6.3 | 工单列表 | 展示工单编号、设备、标题、状态、时间 | P0 |

---

## 4. 技术方向映射表

| 算法模块 | 课程技术方向 | 在系统中的实际作用 | 代码位置 |
|---|---|---|---|
| 随机森林 RUL 预测 | 机器学习（回归） | 健康评估页核心：输入传感器统计特征，输出剩余寿命，驱动预警分级 | `algorithms/rul_rf.py` |
| LSTM RUL 预测 | 深度学习（时序建模） | 用滑动时间窗捕捉退化趋势，与随机森林对比验证 | `algorithms/rul_lstm.py` |
| 故障诊断规则引擎 | 专家系统（IF-THEN 推理） | 智能诊断页核心：把异常征兆转为故障定位与分层排查建议 | `algorithms/rules_engine.py` |
| Isolation Forest 异常检测 | 数据挖掘（异常检测） | 数据监测页标红异常点，并驱动预警中心自动生成预警 | `algorithms/anomaly.py` |

四个模块串成一条链：**异常检测发现问题 → RUL 评估严重度 → 规则引擎给出处置建议 → 工单闭环执行**。

---

## 5. 系统架构

### 5.1 架构图

```
┌─────────────────────────────────────────┐
│  浏览器：Vue 3 + Element Plus + ECharts  │  前端 UI（6 个页面）
└──────────────┬──────────────────────────┘
               │ HTTP / JSON（RESTful API）
┌──────────────▼──────────────────────────┐
│  后端：FastAPI                           │
│  ├─ routers/  各模块 API                 │
│  └─ 调用算法模块做推理                    │
└──────┬──────────────────┬───────────────┘
       │                  │
┌──────▼──────┐   ┌───────▼───────────────┐
│ SQLite      │   │ 模型文件（离线训练好）  │
│ app.db      │   │ rf_rul.pkl /          │
│ （5 张表）   │   │ lstm_rul.pth /        │
│             │   │ isoforest.pkl /       │
│             │   │ scaler.pkl            │
└─────────────┘   └───────────────────────┘
```

模型**离线训练、在线只推理**：训练脚本跑一次存模型文件，后端启动时加载，保证演示响应速度。

### 5.2 技术栈（已冻结）

| 层 | 选型 |
|---|---|
| 前端 | Vue 3 + Element Plus + ECharts |
| 后端 | FastAPI（Python 3.10+），RESTful API |
| 数据库 | SQLite |
| 算法 | scikit-learn + PyTorch（CPU 版） |
| 测试 | pytest（后端接口 + 算法模块） |
| 版本管理 | git + GitHub |

---

## 6. 数据来源

**数据集**：NASA C-MAPSS（涡扇发动机退化仿真数据集），本项目只用 **FD001 子集**（单一工况、单一故障模式：高压压气机 HPC 退化）。

**文件结构**：

| 文件 | 内容 | 用途 |
|---|---|---|
| `train_FD001.txt` | 100 台发动机从健康到失效的完整寿命数据 | 训练模型 |
| `test_FD001.txt` | 100 台只跑到中途的数据（模拟在役设备） | 系统演示数据 |
| `RUL_FD001.txt` | test 集每台设备的真实剩余寿命 | 验证预测精度 |

**数据格式**：纯文本、空格分隔、无表头，每行 26 列：第 1 列 unit（设备编号）、第 2 列 cycle（循环数）、第 3~5 列 setting1~3（工况参数）、第 6~26 列 s1~s21（21 个传感器）。

**RUL 标签构造**：`RUL = 该设备最大 cycle − 当前 cycle`，上限截断为 125。

**获取渠道**：飞桨 AI Studio / 魔搭 ModelScope 搜"C-MAPSS"；或 GitHub 开源仓库。

---

## 7. 非功能需求

| 类别 | 要求 |
|---|---|
| 响应时间 | RUL 预测接口响应 < 2 秒（模型预加载，在线只推理） |
| 自动化测试 | pytest 覆盖后端主要接口与 4 个算法模块，全部通过 |
| 可运行性 | 仓库含 README 与运行脚本，克隆后按文档可一键启动 |
| 可讲解性 | 代码全中文注释、写法朴素，每段代码开发者能现场讲解 |
| 可演示性 | 演示用真实 RUL < 40 的 test 设备，保证业务闭环一条线走通 |
